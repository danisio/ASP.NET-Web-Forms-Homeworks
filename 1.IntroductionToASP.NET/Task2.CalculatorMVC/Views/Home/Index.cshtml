@{
    ViewBag.Title = "Calculator";
}

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <h2>Calculator</h2>
            <form id="calculatorForm" method="post">
                <div class="row">
                    <div class="form-group">Enter number 
                        <input type="text" name="firstNumber" value="@ViewBag.firstNumber" />
                    </div>
                    <div class="form-group">Enter number 
                        <input type="text" name="secondNumber" value="@ViewBag.secondNumber" />
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Calculate" /> <span>@ViewBag.Sum</span>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
